apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-cassandra
  # namespace: user01-seg42
  labels:
    app: pod-cassandra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pod-cassandra
  template:
    metadata:
      labels:
        app: pod-cassandra
    spec:
      # nodeSelector:
        # kubernetes.io/hostname: ids-rj
      volumes:
        - name: cassandradata
          persistentVolumeClaim:
            claimName: cassandradata
      containers:
      - name: pod-cassandra
        image: cassandra:4
        env:
          - name: CASSANDRA_CLUSTER_NAME
            value: TheHive
        volumeMounts:
          - mountPath: /var/lib/cassandra
            name: cassandradata
        ports:
          - containerPort: 9042
            protocol: TCP
