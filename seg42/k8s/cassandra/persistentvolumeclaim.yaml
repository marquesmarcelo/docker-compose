apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    #namespace: user01-seg42
    io.kompose.service: cassandradata
  name: cassandradata
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi