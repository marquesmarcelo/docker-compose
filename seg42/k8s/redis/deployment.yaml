apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-redis
  # namespace: user01-seg42
  labels:
    app: pod-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pod-redis
  template:
    metadata:
      labels:
        app: pod-redis
    spec:
      # nodeSelector:
        # kubernetes.io/hostname: ids-rj
      containers:
      - name: pod-redis
        image: redis:alpine3.22
        ports:
          - containerPort: 6379 
            protocol: TCP