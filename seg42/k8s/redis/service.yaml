apiVersion: v1
kind: Service
metadata:
  name: svc-redis
  # namespace: user01-seg42 (Descomente se estiver usando namespace)
  labels:
    app: redis
spec:
  type: ClusterIP
  selector:
    app: redis 
  ports:
  - name: "redis-api"
    port: 6379
    targetPort: 6379