apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-kali-xrdp
  # namespace: user01-seg42
  labels:
    app: pod-kali-xrdp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pod-kali-xrdp
  template:
    metadata:
      labels:
        app: pod-kali-xrdp
    spec:
      # nodeSelector:
        # kubernetes.io/hostname: ids-rj
      containers:
      - name: pod-kali-xrdp
        image: marquesmarcelo/kali-xrdp:latest
        ports:
          - containerPort: 22 
            protocol: TCP
          - containerPort: 3389
            protocol: TCP